# Backend environment variables (no secrets committed)
# Provide your own keys in a real .env (DO NOT COMMIT .env)

# Gemini API key (optional). Enables nuanced moderation + greeting generation.
GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE

# Upstash Redis REST endpoint + token (optional). Enables durable rate limiting & reaction counts.
UPSTASH_REST_URL=https://your-upstash-url
UPSTASH_REST_TOKEN=your-upstash-rest-token

# Other future keys can be added below.
# Test bypass token (optional). If set, include header X-Test-Bypass: <token> to skip 6h rate limit for QA.
TEST_BYPASS_TOKEN=dev-bypass-token
