<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bong Bari</title>
  <style>
    /* Reset browser default spaces */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: sans-serif;
      background: #fff;
    }

    /* === HEADER === */
    header {
      position: fixed;   /* always stays on top */
      top: 0;
      left: 0;
      width: 100%;
      height: 60px;      /* slim like Flaticon */
      background: #0D47FF;  /* Bong Bari blue */
      color: #fff;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 20px;
      z-index: 1000;
    }

    /* Logo + Text */
    .logo-container {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .logo-container img {
      height: 40px;
      width: 40px;
    }

    .brand-text {
      display: flex;
      flex-direction: column;
      line-height: 1.2;
    }

    .brand-text .title {
      font-size: 16px;
      font-weight: bold;
    }

    .brand-text .tagline {
      font-size: 11px;
      opacity: 0.9;
    }

    /* === NAVIGATION === */
    nav ul {
      list-style: none;
      display: flex;
      gap: 20px;
      align-items: center;
    }

    nav ul li a {
      text-decoration: none;
      color: #fff;
      font-size: 14px;
      transition: color 0.3s ease;
    }

    nav ul li a:hover {
      color: #FFD200; /* Bong Bari yellow */
    }

    /* === PROMO SECTION === */
    .promo-section {
      margin-top: 60px; /* push under header */
      width: 100%;
      background: #FFD200;
      padding: 15px 20px;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .promo-section input {
      width: 50%;
      max-width: 500px;
      padding: 10px;
      border: none;
      border-radius: 4px;
      font-size: 14px;
    }

    /* === RESPONSIVE === */
    .hamburger {
      display: none;
      font-size: 24px;
      cursor: pointer;
    }

    @media (max-width: 768px) {
      nav ul {
        display: none;
      }

      .hamburger {
        display: block;
        color: #fff;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
    <div class="logo-container">
      <img src="your-logo.png" alt="Bong Bari Logo">
      <div class="brand-text">
        <span class="title">বং বাড়ি</span>
        <span class="tagline">Every Home's Story | প্রতিটা বাড়ির গল্প</span>
      </div>
    </div>

    <nav>
      <ul>
        <li><a href="#">Home</a></li>
        <li><a href="#">About</a></li>
        <li><a href="#">Work with us</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#">Blog</a></li>
        <li><a href="#">Login ➝</a></li>
      </ul>
      <div class="hamburger">☰</div>
    </nav>
  </header>

  <!-- PROMO SECTION -->
  <section class="promo-section">
    <input type="text" placeholder="Search offers or promotions...">
  </section>

</body>
</html>
