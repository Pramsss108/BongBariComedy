# Copy to server/.env and fill in your keys (never commit real keys)
###############################################
# Example Environment Variables (server/.env)  #
# Copy this file to server/.env and fill in    #
# secrets. Never commit the real server/.env.  #
###############################################

# Server port (local only)
PORT=5000

# Database (Neon Postgres pooled connection string)
# Format: postgresql://<user>:<password>@<host>/<db>?sslmode=require&channel_binding=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST/dbname?sslmode=require&channel_binding=require

# AI key (server side only; rotate if leaked)
GEMINI_API_KEY=

# Long random auth secret (generate 3 GUIDs concatenated)
JWT_SECRET=

# Comma separated list of allowed origins for CORS
# Include localhost during development + production domain once deployed.
# Example: http://localhost:5173,https://www.bongbari.com
CORS_ORIGIN=http://localhost:5173,https://www.bongbari.com

# (Optional) YouTube channel id if enabling channel related features
YOUTUBE_CHANNEL_ID=

# (Optional) Internal test bypass token (leave blank if unsure)
TEST_BYPASS_TOKEN=

# Production mode hint:
# Render automatically sets NODE_ENV=production. Locally you can omit or set development.
# Do NOT set NODE_ENV=production locally unless you have built the dist output.
#NODE_ENV=production

# Notes:
# - Never put real secrets in version control.
# - After editing server/.env locally restart the dev server.
# - Rotate GEMINI_API_KEY and JWT_SECRET periodically.
