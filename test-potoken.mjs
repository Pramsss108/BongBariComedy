import ytdl from "@distube/ytdl-core"; import { generate } from "youtube-po-token-generator"; async function test() { try { const { visitorData, poToken } = await generate(); const agent = ytdl.createAgent(undefined, { visitorData, poToken }); const info = await ytdl.getInfo("https://youtube.com/shorts/-DkS34UVEGA?si=8PjgZD_mZsA46D0e", { agent }); console.log("Success! Title:", info.videoDetails.title); } catch (e) { console.error("Error:", e.message); } } test();
